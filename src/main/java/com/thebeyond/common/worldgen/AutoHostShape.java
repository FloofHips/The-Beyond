package com.thebeyond.common.worldgen;

/** What the automatic host reads off a vanilla jigsaw structure, so a type is classified without an attempt. */
public interface AutoHostShape {

    boolean the_beyond$foreignPool();

    boolean the_beyond$projects();

    boolean the_beyond$ownHeight();
}
